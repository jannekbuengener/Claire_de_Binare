Closes #3667

## Summary

Untrack four generated `artifacts/**/audit.log` files that caused Docs Hub Guard to fail on `main`.

## Delivered

- Removed from git index: 4 `audit.log` under `artifacts/calibration/` and `artifacts/controlled_lab_evidence/`
- `.gitignore`: explicit `artifacts/**/audit.log` (redundant with `*.log`, documents intent)
- `GITHUB_WORKFLOW_REGISTER.md`: resolved known pre-existing guard failure note

## Brain Evidence

```text
brain_source: repo-only
brain_status: not-used
tools_or_queries:
  - gh run view 28552911397 --log-failed
  - git ls-files artifacts/**/audit.log
records_or_results: none
repo_crosscheck:
  - .github/workflows/docs-hub-guard.yml lines 29-31
  - 4 tracked audit.log matched guard failure output exactly
impact_on_plan: untrack only; report.json/manifest.json bundles unchanged
limitations: no Context Brain query
```

## Guard Failure Evidence

Run `28552911397` on `main` @ `91c6ed0` — step "Block runtime artifacts":
- `artifacts/calibration/2961/pilot_validation/replay-16a0a8f6d92f-0001/audit.log`
- `artifacts/controlled_lab_evidence/run_002/.../audit.log`
- `artifacts/controlled_lab_evidence/run_004/.../audit.log`
- `artifacts/controlled_lab_evidence/run_005/.../audit.log`

## Validation

- `git ls-files "artifacts/**/audit.log"` → empty
- `python -m tools.validate_onboarding_docs` → OK
- `git diff --check` → clean

## Non-goals

- No workflow changes
- No broad artifacts cleanup
- No security alert triage / CVE work
- No CURRENT_STATUS / session log

## Safety Boundaries

- LR NO-GO; repo hygiene only
- Canonical replay evidence (`report.json`, `manifest.json`, etc.) remains tracked

## Residual Work / Restunsicherheits-Intake

- `codeql-python.yml` SARIF/default-setup conflict remains in workflow register known failures
- Code Scanning alerts from PR #3630 close after next successful `security-scan` on main (separate scope)
