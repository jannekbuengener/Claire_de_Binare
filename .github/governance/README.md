# GitHub Governance Gates

Machine-readable files consumed directly by GitHub workflows live here.

`DELIVERY_APPROVED.yaml` is read by `delivery-gate.yml` and remains human-only.
Moving the file into `.github/governance/` does not change its approval state or
authorize deployment. Policies and explanatory governance canon remain under
`knowledge/governance/`.
