node_modules
.dendron.*
build
seeds
.next
pods/service-connections
**/settings.local.json
**/settings.local.json.json
.env
.secrets/
.pytest_cache/
__pycache__/

# Agent Configuration (lokale Entwicklungsumgebung)
# Die lokale Version enthält Windows-spezifische Pfade und sollte nicht committed werden
mcp-config.toml.local

# Agent Workspace (temporäre Dateien)
.cdb_agent_workspace/
