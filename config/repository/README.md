# Repository Policy

[`root_layout.json`](root_layout.json) is the machine-readable allowlist for
tracked root directories and files. Validate it with:

```bash
python -m tools.validate_root_layout
```

The rationale and migration history live in
[`docs/meta/ROOT_INFORMATION_ARCHITECTURE.md`](../../docs/meta/ROOT_INFORMATION_ARCHITECTURE.md).
