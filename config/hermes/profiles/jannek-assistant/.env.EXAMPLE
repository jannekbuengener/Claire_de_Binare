# Secret names only — copy to host /etc/hermes/jannek-assistant.env (never commit values)
OPENROUTER_API_KEY=
ANTHROPIC_API_KEY=
# Dashboard auth for non-loopback binds (prefer Tailscale + loopback instead)
# HERMES_DASHBOARD_USER=
# HERMES_DASHBOARD_PASSWORD=
