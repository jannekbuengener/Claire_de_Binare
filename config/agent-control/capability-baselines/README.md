# Capability Baselines

Inventory hooks for later provider capability probes (`#4254` / `#4255`).

This slice records no live provider capabilities. Unsupported dashboard fields
remain classified as `MANUAL_BOOTSTRAP_ONLY` per
`knowledge/governance/CDB_AGENT_CONTROL_PLANE.md` §7.
