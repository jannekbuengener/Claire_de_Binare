# Versioned Runtime-Run Evidence

Reviewed runtime-run snapshots retained as evidence. New captures are written to
`artifacts/evidence-runs/` by default and become versioned evidence only after an
explicit review and promotion.

Evidence here is scope-local and does not authorize Live-Go or real-money use.
