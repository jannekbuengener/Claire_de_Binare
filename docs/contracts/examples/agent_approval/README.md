# Examples: `cdb.pr_approval_context.v1`

- `positive_approve_recommended.json` — clean App Check Run SUCCESS path
- `negative_mechanism_mismatch.json` — same-name Commit Status must not approve

Generated/validated by unit tests against the live builder when needed.
Fixtures under `tests/fixtures/agent_control/approval/` are the primary
matrix for `#4257`.
